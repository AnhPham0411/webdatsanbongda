web_san_bong/
├── .env                       # Biến môi trường (DB, Auth Secret, Email Config)
├── package.json               # Dependencies (Next.js, Prisma, Nodemailer, etc.)
├── prisma/
│   ├── schema.prisma          # Database Schema (Booking, Voucher, Inquiry, Notification, etc.)
│   └── seed.ts                # Dữ liệu mẫu khởi tạo hệ thống
├── src/
│   ├── actions/               # [SERVER ACTIONS] - Tương tác Database từ Server Component
│   │   ├── auth.ts            # Đăng ký, Đăng nhập, Đăng xuất (NextAuth)
│   │   ├── admin/             # Logic dành cho Quản trị viên
│   │   │   ├── dashboard.ts   # Thống kê doanh thu, tỷ lệ lấp đầy sân
│   │   │   ├── categories.ts  # CRUD Loại sân (Sân 5, 7, 11)
│   │   │   ├── courts.ts      # CRUD Sân bóng & Gửi thông báo sân mới
│   │   │   ├── bookings.ts    # Quản lý đơn đặt sân, duyệt bill, hủy đơn
│   │   │   ├── voucher.ts     # CMS Voucher: Tạo mã, gửi chuyên biệt cho Top Chi tiêu
│   │   │   ├── settings.ts    # Cấu hình website (Gồm Chế độ bảo trì, Logo, Tên, Giờ mở cửa)
│   │   │   ├── reviews.ts     # Admin phản hồi đánh giá của khách
│   │   │   ├── calendar.ts    # Lấy dữ liệu lịch đặt sân (TimeSlots)
│   │   │   ├── users.ts       # Quản lý người dùng và phân quyền (Admin, Staff, User)
│   │   │   ├── amenities.ts   # CRUD Tiện ích đi kèm (Wifi, Căng tin, Parking)
│   │   │   └── locations.ts   # Quản lý cụm sân / vị trí địa lý
│   │   └── client/            # Logic dành cho Người dùng
│   │       ├── booking.ts     # Đặt sân, áp dụng voucher, hỗ trợ giữ chỗ 10p
│   │       ├── voucher.ts     # Check mã giảm giá & Lấy voucher của tôi
│   │       ├── contact.ts     # Gửi yêu cầu hỗ trợ (Inquiry)
│   │       ├── reviews.ts     # Đánh giá sân sau khi trải nghiệm
│   │       ├── notifications.ts # Quản lý thông báo
│   │       └── search.ts      # Logic tìm kiếm & Filter sân nâng nâng cao
│   ├── app/                   # [ROUTING - Next.js App Router]
│   │   ├── (admin)/           # Dashboard Quản trị (Protected Routes)
│   │   │   └── admin/
│   │   │       ├── bookings/  # Duyệt thanh toán (Xem ảnh Bill)
│   │   │       ├── inquiries/ # Trình phản hồi khách hàng (CRM)
│   │   │       ├── dashboard/ # Biểu đồ doanh thu Recharts
│   │   │       └── settings/  # Cấu hình tham số hệ thống
│   │   ├── (public)/          # Giao diện người dùng
│   │   │   ├── courts/[id]/   # Booking Engine & Review Section
│   │   │   └── my-bookings/   # Quản lý cá nhân & Upload minh chứng thanh toán
│   ├── components/            # [UI COMPONENTS] - Reusable Shaden UI & Custom
│   │   ├── admin/             # InquiryReplyForm, BookingActions, Sidebar
│   │   └── client/            # CountdownClock, VoucherCard, NotificationBell
│   ├── lib/                   # [LIBRARY & CONFIG]
│   │   ├── db.ts              # Prisma Client Instance
│   │   ├── mail.ts            # Nodemailer integration (Send email confirmation)
│   │   └── auth.ts            # NextAuth Configuration (JWT, Callbacks)

# Cơ chế nghiệp vụ đặc biệt (Nâng cao):
1. Giữ chỗ thông minh (Smart Hold): Khóa slot 10p khi PENDING -> Auto mở nếu không upload bill.
2. Xác nhận 2 bước: Khách upload ảnh Bill (Xác nhận ảo) -> Admin duyệt (Xác nhận thực).
3. Voucher & Ranking Engine: Tự động xếp hạng Top 3 chi tiêu, hỗ trợ tìm kiếm và gán mã chuyên biệt.
4. Chế độ bảo trì (Maintenance Mode): Chặn truy cập Giao diện & khóa API đặt sân cho khách, chỉ cho phép Admin hoạt động.
5. Auth Normalization: Tự động chuẩn hóa Email (viết thường/xoá khoảng trắng) để sửa lỗi đăng nhập.
6. CRM tích hợp: Hệ thống Inquiry trực tuyến giúp phản hồi khách hàng ngay trên Dashboard.